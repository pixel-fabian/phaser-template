import "phaser";
export class SceneGame extends Phaser.Scene {

    constructor() {
        super({
            key: "SceneGame"
        });
    }

    //////////////////////////////////////////////////
    // LIFECYCLE (init, preload, create, update)    //
    //////////////////////////////////////////////////

    init(params): void {
    }

    preload(): void {
    }
    
    create(): void {
    }

    update(time): void {
    }

    //////////////////////////////////////////////////
    // Private methods                              //
    //////////////////////////////////////////////////
};